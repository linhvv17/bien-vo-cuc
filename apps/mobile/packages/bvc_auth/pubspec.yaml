name: bvc_auth
description: Auth contracts & stubs (extend with JWT/session later)
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.4
  dio: ^5.9.2
  bvc_network:
    path: ../bvc_network
  flutter_modular: ^6.4.1
  bvc_ui:
    path: ../bvc_ui
